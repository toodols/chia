fn print(value: number) -> () {}
fn to_number(from: string) -> number { 0 }

fn main() -> number {
	let my_number = 1;
	let something = "abcdef";
	let something = to_number(something);
	print(something);
	my_number
}

/*
0 (program): {
	1 (fn print) {
		var value: number
	}
	2 (fn main) {
		var my_number: number
		var something: string
		
		3 (let something) {
			var something: number

			<end of fn main>
		}
	}
}

*/