// test code to make sure compiler works

fn sum(left: number, right: number) -> number {
	left + right
}

fn print(text: string) -> () {}
fn num_to_string(num: number) -> string {
	return "abc";
}


fn main(){
	print("hello");
	let text = num_to_string(sum(1, 2));
	let a = if text == "3" {
		1==2 + 1		
	} else {
		2==2
	};
}